# oauth2-proxy Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to git!

# =============================================================================
# IMPORTANT: Use M2M (Machine-to-Machine) App credentials, NOT the SPA/Mobile app!
# =============================================================================
# The regular app (FamilyHelperAPPDev/Prod) is a "public client" with no secret.
# oauth2-proxy requires a "confidential client" which has a client secret.
# Create a separate M2M app in Kinde for oauth2-proxy to use.

# Kinde M2M App Client ID
# Dev (FamilyHelperAPPM2MDev): 6363c85be6ba43529f5bcd953f5fdb32
# Prod (FamilyHelperAPPM2MProd): TODO - Create when ready for production
OAUTH2_PROXY_CLIENT_ID=6363c85be6ba43529f5bcd953f5fdb32

# Kinde M2M App Client Secret
# Get from: https://familyhelperapp.kinde.com → Settings → Applications → M2M App
OAUTH2_PROXY_CLIENT_SECRET=your-m2m-client-secret

# Cookie encryption secret (32 bytes)
# Generate with: openssl rand -base64 32 | tr -d '\n'
OAUTH2_PROXY_COOKIE_SECRET=generate-a-random-32-byte-string
