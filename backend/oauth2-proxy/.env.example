# oauth2-proxy Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to git!

# =============================================================================
# IMPORTANT: Use Back-end Web App credentials, NOT the SPA/Mobile app!
# =============================================================================
# The regular app (FamilyHelperAPPDev/Prod) is a "public client" with no secret.
# oauth2-proxy requires a "confidential client" which has a client secret.
# Use the Back-end Web app in Kinde for oauth2-proxy.

# Kinde Back-end Web App Client ID
# Dev (FamilyHelperBackEndWebAPP): de8522625a2048fd8b38a5307b20090e
# Prod (FamilyHelperBackEndWebAPPProd): aa8f22665e164787ac554f97c1138ee0
OAUTH2_PROXY_CLIENT_ID=aa8f22665e164787ac554f97c1138ee0

# Kinde Back-end Web App Client Secret
# Get from: https://familyhelperapp.kinde.com → Settings → Applications → Back-end Web App
# NEVER commit the actual secret - only store on server!
OAUTH2_PROXY_CLIENT_SECRET=your-backend-web-app-client-secret

# Cookie encryption secret (32 bytes)
# Generate with: openssl rand -base64 32 | tr -d '\n'
OAUTH2_PROXY_COOKIE_SECRET=generate-a-random-32-byte-string
